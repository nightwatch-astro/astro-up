use crate::detect::{DetectionResult, PathResolver};
use crate::types::DetectionConfig;

pub async fn detect(_config: &DetectionConfig, _resolver: &PathResolver) -> DetectionResult {
    todo!("T014: implement PE file detection")
}
