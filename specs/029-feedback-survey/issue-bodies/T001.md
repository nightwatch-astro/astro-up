## Task

Add `survey_threshold` (u32, default 3), `survey_dismissed_at` (Option<String>), `survey_completed_at` (Option<String>) fields to `UiConfig` struct.

## File

`crates/astro-up-core/src/config/model.rs`

## Acceptance Criteria

- Three new fields on `UiConfig` with appropriate defaults (threshold=3, timestamps=None)
- Fields serialize/deserialize correctly via serde
- `known_keys()` returns the new dot-path keys

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 1: Foundational)
