## Task

Add `count_successful_operations()` function — SQL `COUNT(*)` on operations table filtered by `status = 'success'` AND `operation_type IN ('install', 'update')`.

## File

`crates/astro-up-core/src/engine/history.rs`

## Acceptance Criteria

- Returns `Result<u64, CoreError>`
- Counts only successful install and update operations
- Excludes failed, cancelled, reboot_pending, and uninstall operations

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 1: Foundational)
FR-001, FR-011
