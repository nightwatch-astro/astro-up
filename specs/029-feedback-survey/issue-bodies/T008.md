## Task

Create `SurveyDialog.vue` — PrimeVue Dialog (modal, 480px) with icon, "How's Astro-Up working for you?" message, and 3 buttons.

## File

`frontend/src/components/shared/SurveyDialog.vue`

## Behavior

- "Leave feedback" (primary button): invoke `complete_survey`, then `openUrl('https://tally.so/r/lb7dd5')`
- "Not now" (text/secondary): invoke `dismiss_survey`
- "Don't ask again" (text/secondary): invoke `complete_survey`
- Escape / click outside modal: invoke `dismiss_survey` (treat as "Not now")
- Follow `ConfirmDialog.vue` pattern for structure and styling

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 2: US1)
FR-003, FR-004, FR-005, FR-006
