## Task

Add three Tauri commands: `check_survey_eligible` (returns bool), `dismiss_survey` (sets dismissed_at to now), `complete_survey` (sets completed_at to now).

## File

`crates/astro-up-gui/src/commands.rs`

## Acceptance Criteria

- `check_survey_eligible` reads config + counts operations via core functions
- `dismiss_survey` sets `ui.survey_dismissed_at` to current UTC timestamp
- `complete_survey` sets `ui.survey_completed_at` to current UTC timestamp
- All commands follow existing error handling pattern (return `Result<_, CoreError>`)
- Logging: `info!` on dismiss/complete, `debug!` on eligibility check

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 2: US1)
FR-002, FR-005, FR-006
