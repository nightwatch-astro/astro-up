## Task

Add `set_field`/`get_field_value` match arms for `ui.survey_threshold`, `ui.survey_dismissed_at`, `ui.survey_completed_at`.

## File

`crates/astro-up-core/src/config/mod.rs`

## Acceptance Criteria

- `set_field("ui.survey_threshold", "5")` updates threshold
- `set_field("ui.survey_dismissed_at", "2026-04-09T00:00:00Z")` stores timestamp
- `get_field_value("ui.survey_completed_at")` returns stored value or None
- Follows existing pattern for other config fields

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 1: Foundational)
FR-008
