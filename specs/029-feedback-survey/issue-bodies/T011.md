## Task

Add integration test verifying snooze expiry triggers re-eligibility.

## File

`crates/astro-up-core/src/config/mod.rs` (tests module)

## Test Cases

- Set `ui.survey_dismissed_at` to 31 days ago → `check_survey_eligible` returns `true`
- Set `ui.survey_dismissed_at` to 29 days ago → `check_survey_eligible` returns `false`

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 4: US3)
SC-003
