## Task

Register `check_survey_eligible`, `dismiss_survey`, `complete_survey` in the `invoke_handler` macro.

## File

`crates/astro-up-gui/src/lib.rs`

## Acceptance Criteria

- All 3 commands added to `tauri::generate_handler![]` list
- App compiles and commands are callable from frontend

## Spec Context

Part of spec 029 — User Feedback Survey (Phase 2: US1)
